{
  "name": "nft1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ganache": "npx ganache-cli --deterministic",
    "migrate": "npx truffle migrate --network development",
    "compile": "npx truffle compile",
    "console": "npx truffle console --network development",
    "maticmigrate": "npx truffle migrate --network matic -f 6 --to 6 --reset",
    "maticconsole": "npx truffle console --network matic"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.0.8",
    "truffle": "^5.5.11"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "ganache-cli": "^6.12.2"
  }
}
